apiVersion: apps/v1
kind: Deployment
metadata:
  name: cilist-fe
  labels:
    app: cilist-fe
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cilist-fe
  template:
    metadata:
      labels:
        app: cilist-fe
    spec:
      volumes:
      - name: volume-frontend-env
        configMap:
          name: cilist-configmap-fe
      containers:
      - name: frontend
        image: profesorgreen36/cilist_frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        volumeMounts:
        - name: volume-frontend-env
          mountPath: "/app/.env"
          subPath: ".env"
        env:
        - name: BACKEND_URL
          value: "backend:5000"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: NodePort
  selector:
    app: cilist-fe
  ports:
  - port: 3000
    targetPort: 3000